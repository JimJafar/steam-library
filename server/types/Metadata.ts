type Metadata = {
  id: number,
  metacriticUrl?: string,
  metacriticScore?: number,
  steamScore?: number,
  steamReviewCount?: number,
  onMac: boolean,
  onDeck: string,
}

export default Metadata
